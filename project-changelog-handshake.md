# Project Changelog Handshake

## System Metadata
- Target Goal: **Freelanz** — a local-first freelance automation PWA (invoicing, tax, doc-gen, booking, CRM, portfolio) for independent workers, English-only MVP, bilingual (EN/TH) on the roadmap.
- Architecture: local-first PWA — vanilla JS single `app.js`, IndexedDB, Service Worker, no backend, no secrets. Borrows proven scaffolding from DriverLog (`../Driver Log/site/`).
- Identity: teal `#0F766E`/`#14B8A6` + marigold `#F59E0B` accent, warm surface `#FBFAF7`, dark theme.
- Orchestration mode: **ultracode** for (a) how agents run, (b) adversarial verification, (c) dynamic skill/tool loading.
- Status: **M4 COMPLETE (verified in-browser)** · v0.4.0 · all scoped milestones done — M-AI remains explicitly deferred (scope TBD, needs a user decision, see milestone note)
- Last Sync Timestamp: 2026-07-09T16:00:00+07:00
- Local dev server: `cd app && python3 -m http.server 8823` → http://localhost:8823/ (SW/IndexedDB need http, not file://)
- IndexedDB name: `freelanz-v2` (renamed from freelanz-v1 during M1.5 verify; no prior real users).

## Milestones
- [x] **M1 — Foundation**: PWA shell, auth+guest (PBKDF2), IndexedDB, persona onboarding, i18n `t(key@persona)` relabel layer, theming, dashboard, log-job + settings + CSV/JSON export. **DONE + verified.**
- [x] **M1.5 — Refinements**: light-mode-only (toggle removed), job-type dropdown + Custom + Personal gym trainer, Customers module (details), Services catalog w/ rates + fee-prefill. **DONE + browser-verified.**
- [x] **M2 — Money & docs**: invoicing + PromptPay QR, WHT/VAT tax engine, doc-gen templates. **DONE + browser-verified.**
- [ ] **M-AI (backend, later)**: Vercel serverless proxy holding the AI Gateway key (env var) for an AI feature — scope TBD. Key must NEVER be client-side. User's pasted key was exposed in chat → rotate before use. **Blocked on user scope decision — no concrete feature spec exists yet, so nothing to build.**
- [x] **M3 — Booking & CRM**: day view + travel buffers, follow-up queue, portfolio. **DONE + browser-verified.**
- [x] **M4 — Polish**: offline hardening (audited + live-verified, no gaps found), CSV/JSON export (fixed a real gap — backup silently missed 7 of 9 stores), deploy prep (`.htaccess` + subpath-mount live-verified). **DONE + browser-verified.** Actual push-to-Hostinger is a user action (needs their hosting credentials) — everything buildable/testable in this environment is done.

## Reuse Ledger (borrowed from sibling projects)
| System | Source | Disposition |
|---|---|---|
| IndexedDB layer (`openDB/dbAll/dbPut/dbAdd/dbDel/dbGet`) | DriverLog `site/app.js` | lift + add `clients/invoices/documents` stores |
| Auth + guest (`sha256hex/randomSalt/hashPassword/loginGuest/submitAuth/logout`) | DriverLog | lift verbatim |
| i18n engine `t(key@type)` + `applyLang` | DriverLog | lift engine, re-author dict (EN only, 5 personas) |
| Persona onboarding + presets | DriverLog `applyWorkerType` | adapt to 5-persona taxonomy |
| CSV(BOM, injection-safe) + JSON backup/restore | DriverLog | lift verbatim |
| PWA shell / `sw.js` / manifest / subpath build | DriverLog `site/` | lift + rebrand |
| Deploy discipline (version lockstep, no-cache, versioned URLs, Hostinger MCP) | global CLAUDE.md + memory | apply as process |

**NOT borrowed** (handoff §10 + secrets rule): DriverLog brand/red, AdSense id, AccessTrade affiliate slider, `drivee-orchestrator`, PocketBase sync, any tokens/FTP creds.

## Model / Effort policy (approved 2026-07-08, effective M2 onward)
Right-size per task — Opus on the crux, cheaper tiers on mechanical checks:
| Task kind | Model · Effort |
|---|---|
| Builder (multi-file / migration / new screens) | **Opus · high** |
| Verify — data-loss / DB-migration risk | **Opus · high** |
| Fix — applies edits to shared/crux code | **Opus · high** |
| Verify — i18n / UX / handler-exists (logic trace) | **Sonnet · high** |
| Verify — version-lockstep / grep-mechanical | **Haiku · medium** |
_M1.5 ran pre-policy (all Opus·high). M2 and M3 both applied this matrix via the `Agent` tool's `model` param (builders on Opus; verify lenses split Opus/Sonnet/Haiku by risk tier)._

## Delta Change Logs
- 2026-07-08 · orchestrator · INIT · Handshake created. Wireframes (M0) approved as-is. Scope locked: local-first PWA MVP, English-only. Launching M1 build via ultracode workflow (build → 5-lens adversarial verify → fix).
- 2026-07-08 · ultracode-wf `wf_2c61b339-ade` · BUILD · 7 files written to app/ (index.html, login.html, app.js, styles.css, sw.js, manifest.json, icons/icon.svg). DriverLog spine lifted (IDB/auth/i18n/CSV+JSON export/boot); stripped brand/AdSense/affiliate/PocketBase-sync/driver-fields/secrets. node --check + stubbed-DOM runtime tests 21/21 pass. APP_VERSION 0.1.0 / SW freelanz-v0.1.0.
- 2026-07-08 · verify-panel (5 lenses) · VERIFY · 11 findings, 0 high/critical. Auto-fixer applied none (gated to high/critical).
- 2026-07-08 · orchestrator + fix-agent · FIX · Curated 9 fixes applied: importBackup atomicity (data-loss), SHA-256→PBKDF2(100k)+min-pw-8, visible focus rings, form `for`/labels, auto-theme dark contrast (module-pill + settings-input), SW no longer caches 4xx/5xx, delete_job@persona variants, restore no longer bleeds theme/lang. Deferred (2): guest-persistence (intentional per handoff), iOS PNG apple-touch-icon → M4. node --check green.
- 2026-07-08 · orchestrator · LIVE-CHECK · Drove real Chrome @ localhost:8823: guest → onboarding → Photographer persona relabel (live) → log shoot (Net ฿10,000 computed) → dashboard stats update → Shoots list row → full-reload persistence. 0 console errors. M1 signed off.
- 2026-07-08 · ultracode-wf `wf_973fe4b7-91c` · M1.5 BUILD+VERIFY+FIX · 5 files edited in place; DB_VER 1→2 adds `services` store (guarded, no data loss). Verify (4 lenses): 3 findings, 1 high (customer-intake data-loss) auto-fixed. Model/effort: all Opus·high (ran pre-policy).
- 2026-07-08 · orchestrator · M1.5 FIXES · Applied 2 remaining verify lows (job-type-preserve-on-edit; pre-paint light inline script) + 2 self-found robustness fixes during live check: `openDB` `onblocked`/`onversionchange` (multi-tab upgrade could wedge — real), boot() error-surface instead of silent blank. Bumped v0.2.0→0.2.1 (lockstep). Renamed DB freelanz-v1→v2 (v1 got wedged locally by aggressive debug delete-while-open).
- 2026-07-08 · orchestrator · M1.5 LIVE-CHECK · Clean Chrome run: fresh boot→login→**light mode** (no toggle) → guest → job-type **dropdown** → gym persona relabel (Sessions/Member) → **services seeded** (gym-only, correct) → add-session **service prefills fee ฿800** → **saves + persists**, dashboard ฿800/1. 0 console errors. Note: two transient anomalies during debugging were self-inflicted test-state pollution, disproven by clean run. M1.5 signed off.
- 2026-07-08 · orchestrator · DIRECTION · User adopted ultracode for agents/verify/dynamic-skills; approved per-task model/effort matrix (see above). Vercel = later serverless-proxy AI feature (backend). Next: secrets .gitignore → git init → GitHub via gh.
- 2026-07-09 · ultracode-wf · M2 BUILD+VERIFY+FIX (commit `f082abc`) · Parallel module fan-out over a shared scaffold contract: tax.js (WHT/VAT `computeTax`, TH-convention math) ∥ invoices.js (quote→invoice, line items w/ service prefill, deposit, statuses, auto-numbered `INV-YYYY-####` per-uid, detail/print view) ∥ docgen.js (Contract/NDA/Quote from customer intake, print/export). PromptPay: self-contained EMVCo payload + CRC16-CCITT + QR encoder, fully offline. Adversarial verify found + fixed: PromptPay 0066-mobile misclassification, a11y (labels/focus/keyboard), print-CSS scoping, version-lockstep. `invoices`/`documents` IndexedDB stores added (guarded `contains()` checks; `DB_VER` stayed at 2 — see backlog). Light-mode only, no backend, no secrets. Bumped v0.2.1→0.3.0 (lockstep across app.js/sw.js/index.html/login.html).
- 2026-07-09 · orchestrator · LIVE-CHECK · Drove real Chromium @ localhost:8823: guest → onboarding → set PromptPay ID → add customer (Acme Co) → add service (Consulting ฿5,000/hour, gym-seed defaults intact) → new invoice, picked client + line item (2×฿2,500) → correct VAT(7%)/WHT(3%) breakdown → saved as **INV-2026-0001** → detail view renders a scannable PromptPay QR canvas → Tax screen breakdown computes live → Docs screen → New Contract form opens pre-filled with customer/date. 0 console errors across the full flow. M2 signed off.
- 2026-07-09 · orchestrator · M3 SCAFFOLD · Shared contract laid before fan-out: `DB_VER` 2→3 adds `bookings`/`followups`/`portfolio` stores (guarded `!contains()`, no data loss) — this same bump also retroactively backfills the `invoices`/`documents` stores for any pre-existing v2 database, closing the M2 gap logged below (onupgradeneeded now fires again since the version actually increased). Wired `s-book`/`s-followups`/`s-portfolio` screens, two new More-menu rows, and three `<script>` tags. Bumped v0.3.0→0.4.0 lockstep across app.js/sw.js/index.html/login.html.
- 2026-07-09 · 3 parallel Opus·high builder agents · M3 BUILD · Independent module fan-out over the scaffold: bookings.js (day view with prev/today/next nav, per-day agenda sorted by start time, travel-buffer warning strips between adjacent bookings, add/edit/delete modal) ∥ followups.js (live-computed CRM queue — overdue invoices, unsent drafts >3d, stale customers >30d — with per-item snooze-7d/dismiss backed by a dedicated `followups` decision store) ∥ portfolio.js (item grid with 2MB-capped client-side image-to-dataURL, tags, add/edit/delete, Share/Print view reusing docgen.js's print-root mechanism under its own `pf-` prefix). All three `node --check` clean on first pass.
- 2026-07-09 · 3 parallel verify agents (Opus·high data-loss/DB-migration, Sonnet·high i18n/UX/logic-trace, Haiku·medium version-lockstep) · VERIFY · 0 critical/high findings — DB migration backfill confirmed genuine (not just claimed), uid scoping consistent across all three modules, no cross-account leaks, image-size cap runs before the read, follow-up dedup keys collision-free, no writes into the read-only `invoices` store. 1 mechanical FAIL (stale hardcoded "0.3.0" version-display text) + several real medium/low findings from the logic lens.
- 2026-07-09 · orchestrator · M3 FIX · Applied 6 curated fixes: version-display text 0.3.0→0.4.0; bookings.js travel-buffer strip was keyed to the row *before* it and could render next to the wrong pair when a cancelled booking sits between two active ones — rekeyed to the row it precedes; overnight bookings (end past midnight) now show a "(+1d)" suffix; a 0-buffer overlap no longer says "need 0 min buffer"; followups.js now also surfaces an invoice manually marked "overdue" with a blank due date (previously only computed from `dueDate < today`); bookings.js/followups.js render functions now guard their data load with try/catch matching portfolio.js's existing pattern instead of leaving a blank screen on a rejected `dbAll()`. Deferred to backlog (see below): no un-dismiss for follow-up items, cross-midnight booking-conflict detection, swallowed delete errors (inherited from invoices.js's existing pattern, not a regression).
- 2026-07-09 · orchestrator · M3 LIVE-CHECK · Drove real Chromium @ localhost:8823: guest → add customer → **Book** tab → new booking (Studio shoot, 09:00–10:00, 30min buffer) → second booking overlapping it (09:45–10:15) → travel-buffer warning renders correctly ("⚠ Overlaps by 15 min — need 30 min buffer") → More → **Follow-ups** (empty state correct, no history yet) → More → **Portfolio** → add item with tags → renders as a card with chips + Share/Print button appears. Separately: created an invoice with a past due date + status "sent" → Follow-ups screen correctly surfaced "Invoice INV-2026-0001 is 38 days overdue" with working Snooze-7d (re-tested: item disappears + "Snoozed for 7 days" toast) and Dismiss actions. 0 console errors across every screen and action. M3 signed off.
- 2026-07-09 · orchestrator · M4 ASSESS · Audited the three M4 line items before building anything. Found the highest-value real gap: `exportBackup()`/`importBackup()` (from M1) only ever round-tripped `jobs`/`expenses` — every store added since M1.5/M2/M3 (clients, services, invoices, documents, bookings, followups, portfolio) was silently excluded from both "Backup JSON" and restore, with no UI indication of the gap. Offline (sw.js) and deploy-readiness (BASE-derived subpath support) looked solid on inspection; verified rather than assumed (see LIVE-CHECK below).
- 2026-07-09 · orchestrator · M4 FIX — export · Generalized `exportBackup()`/`importBackup()` over a `BACKUP_STORES` list (all 9 uid-scoped stores), preserving the original delete-all-then-add-all-with-rollback atomicity exactly (just extended across more stores, not redesigned). Old-format backups missing newer keys default those stores to empty on restore (backward compatible). Added "Export invoices CSV" (number/dates/client/status/subtotal/VAT/WHT/totals) alongside the existing jobs/customers CSV exports.
- 2026-07-09 · orchestrator · M4 LIVE-CHECK — export · Seeded one row in each of clients/invoices/bookings/portfolio, exported Backup JSON, confirmed the file actually contains all 9 store keys with the right row counts, then deleted all four rows via the UI, restored from that same file, and confirmed all four reappeared correctly (screenshotted). Invoices CSV export downloaded and its contents matched the invoice on screen. 0 console errors.
- 2026-07-09 · orchestrator · M4 LIVE-CHECK — offline · Booted the app online (installs the SW, precaches the shell, opens IndexedDB), reloaded so the page came under SW control, then went fully offline (`context.setOffline(true)`) and reloaded again: the app booted from cache with 0 console errors, every M2/M3 screen (Book/Invoices/Docs) rendered, and — the real test — creating a brand-new booking while offline (a pure IndexedDB write, zero network) succeeded and displayed immediately. A "cold" direct navigation to the app root while still offline also rendered correctly. No sw.js changes were needed; the existing cache-first/navigate-fallback strategy already covered this correctly.
- 2026-07-09 · orchestrator · M4 FIX + LIVE-CHECK — deploy prep · Added `.htaccess` for Apache/Hostinger-style shared hosting: no-cache on the HTML shell + `sw.js` (so a bumped version is picked up promptly), a day of caching on everything else, gzip, directory listing off — no rewrite rules needed since this isn't a server-routed app. Then actually served a full copy of `app/` from a `/freelanz/` subpath (not domain root) and drove it end to end: the service worker registered with scope `.../freelanz/` (not root), guest login redirected correctly within the subpath, and an offline reload still rendered the whole app — confirming the BASE-derived-from-sw-location design is genuinely subpath-safe, not just documented as such. Actual upload to the live Hostinger account is a user action (needs their hosting credentials, which this sandboxed session doesn't have) — everything buildable/verifiable here is done. M4 signed off.

## Known polish backlog (non-blocking, from live check + verify)
- **Not yet done: pushing the built app to the live Hostinger account.** `.htaccess` + subpath-mount are prepped and verified (see M4 delta log); the actual upload needs the user's Hostinger credentials/action, which this sandboxed session doesn't have. Everything on the engineering side is ready to go.
- iOS needs a raster (PNG) apple-touch-icon + 192/512 manifest PNGs → bundle whenever deploy actually happens. (low)
- Shoots/jobs list row shows client name twice (title + subtitle) when jobType is empty — use jobType or "Shoot" as title. (low)
- Guest data persists on shared device by design; consider an explicit "start fresh" vs "resume" prompt later. (low)
- ~~`DB_VER` stayed at 2 when M2 added `invoices`/`documents` stores~~ — **fixed in M3**: `DB_VER` bumped to 3, which backfills those stores for any pre-existing v2 database too.
- Follow-up queue items have no "un-dismiss": once a user taps Dismiss, that specific overdue-invoice/draft/stale-customer nudge can never reappear (the underlying invoice/customer record itself is untouched — only the reminder is gone for good). Consider a "Dismissed" sub-list with an undo action. (low)
- bookings.js only compares bookings within the same calendar day for travel-buffer/overlap conflicts — an overnight booking that runs past midnight isn't checked against the next day's early-morning bookings. (low, edge case)
- bookings.js/portfolio.js delete actions swallow a failed `dbDel` and toast success unconditionally (inherited from invoices.js's existing `deleteInvoice` pattern, not a regression introduced in M3 — `dbDel` never actually rejects today, so this is theoretical). (low)
